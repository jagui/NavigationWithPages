<PageFunction  
    x:Class="NavigationWithPages.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      x:TypeArguments="sys:Boolean"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Login">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock>Username</TextBlock>
        <TextBlock Grid.Column="1" x:Name="Username"></TextBlock>
        <TextBlock Grid.Row="1">Password</TextBlock>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="Password"></PasswordBox>
        <Button Grid.ColumnSpan="2" Grid.Row="2" HorizontalAlignment="Center" Click="Next">Next</Button>
    </Grid>
</PageFunction>
